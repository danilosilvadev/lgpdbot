// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyDlzyJ-ndNt76fNd-WnOGqkLcy8aWd0u0I",
    authDomain: "lgpd-api.firebaseapp.com",
    databaseURL: "https://lgpd-api.firebaseio.com",
    projectId: "lgpd-api",
    storageBucket: "lgpd-api.appspot.com",
    messagingSenderId: "126527167525",
    appId: "1:126527167525:web:bea616dac5d9458fee33cc",
    measurementId: "G-HY7HBDRJMP"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
